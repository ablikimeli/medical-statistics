# 医学统计学教程

> **版本**: v1.0  
> **更新时间**: 2026年4月  
> **适用人群**: 医学生、临床医生、医学研究人员、公共卫生从业者

---

## 目录

- [前言：医学统计学的核心价值与学习目标](#前言)
- [第一部分：医学统计学基础](#第一部分)
- [第二部分：核心医学统计学工具](#第二部分)
- [第三部分：统计工具实操](#第三部分)
- [第四部分：常见错误与注意事项](#第四部分)
- [第五部分：总结与拓展](#第五部分)
- [附录：常用统计工具速查表](#附录)

---

# 前言：医学统计学的核心价值与学习目标

## 1.1 核心价值

医学统计学是连接**医学研究**（临床试验、基础实验、流行病学调查）与**科学结论**的桥梁。其核心价值体现在：

| 价值维度 | 具体体现 |
|---------|---------|
| **研究设计优化** | 样本量估算、随机化分组、对照设置 |
| **数据可靠性保障** | 质量控制、偏倚识别、结果验证 |
| **结论外推能力** | 从样本推断总体，从现象揭示规律 |
| **临床决策支持** | 循证医学、诊疗指南制定 |

> **核心问题**: "数据如何支撑医学结论？"、"如何避免数据误导？"

医学统计学贯穿医学研究的全流程：研究设计 → 数据收集 → 分析解读 → 论文报告。

## 1.2 学习目标

完成本教程学习后，您将能够：

1. **概念掌握**: 区分数据类型，理解核心统计术语（总体与样本、P值、置信区间）
2. **工具运用**: 根据研究设计和数据类型，匹配正确的统计分析方法
3. **独立分析**: 使用SPSS/R/Stata完成基础统计分析并正确解读结果
4. **规避错误**: 识别并避免常见统计错误
5. **论文规范**: 按照国际标准（STROBE、CONSORT）规范报告统计结果

---

# 第一部分：医学统计学基础

## 1.1 数据类型

### （1）计量资料（定量资料）

**定义**: 用连续数值表示的资料，具有计量单位。

**特征**: 
- 数值连续，可取任意值（如1.75m、37.5℃）
- 可进行加减乘除运算
- 通常服从正态分布或可转换为正态分布

**常见示例**:

| 指标 | 单位 | 特点 |
|------|------|------|
| 身高，体重 | cm, kg | 近似正态分布 |
| 血压（收缩压/舒张压） | mmHg | 近似正态分布 |
| 血糖、血脂 | mmol/L | 可能偏态 |
| 体温 | ℃ | 近似正态分布 |
| 肝功能指标（ALT、AST） | U/L | 常偏态分布 |

**正态性判断**:
- **图示法**: 直方图、P-P图/Q-Q图
- **统计检验**: Shapiro-Wilk检验（n<50）、Kolmogorov-Smirnov检验（n≥50）
- **经验法则**: 偏度(Skewness)绝对值<2，峰度(Kurtosis)绝对值<7

### （2）计数资料（定性资料）

**定义**: 按事物属性分组清点数目得到的资料。

**分类**:

| 类型 | 定义 | 示例 | 编码 |
|------|------|------|------|
| **二分类** | 只有两个互斥类别 | 性别（男/女）、生死、患病/未患病 | 0/1 |
| **无序多分类** | 多个互斥类别，无等级关系 | 血型（A/B/AB/O）、民族 | 1/2/3/4 |
| **有序多分类** | 多个类别，存在等级顺序 | 疗效（治愈/有效/无效）、病情（轻/中/重） | 1/2/3 |

### （3）等级资料（有序资料）

**定义**: 具有等级顺序但非定量的资料。

**特点**:
- 介于计量与计数之间
- 只能比较大小，不能进行加减运算
- 如：文化程度（文盲/小学/中学/大学）

> **特别注意**: 等级资料的统计分析应选用非参数检验或专门的有序回归模型，不能简单当作计数资料处理。

## 1.2 核心术语

### （1）总体与样本

```
总体 (Population)
├── 总体参数（Parameter）：μ, σ, π
└── 研究者想要推断的全部观察对象的集合

抽样 ↓

样本 (Sample)
├── 样本统计量（Statistic）：x̄, s, p̂
└── 从总体中随机抽取的部分观察对象

统计推断 ↓

用样本统计量推断总体参数
```

### （2）误差类型

| 误差类型 | 定义 | 控制方法 |
|---------|------|---------|
| **随机误差** | 由个体变异和测量误差引起，不可避免 | 增加样本量、重复测量 |
| **系统误差** | 由仪器、试剂、方法等系统性偏差引起 | 校准仪器、标准化方法 |
| **过失误差** | 人为操作失误（如抄录错误） | 严格质量控制 |

### （3）P值（p-value）

**定义**: 在原假设（H₀）成立的条件下，观测到当前结果或更极端结果的概率。

**解读原则**:
- P < 0.05：拒绝H₀，结果有统计学意义
- P ≥ 0.05：不拒绝H₀，结果无统计学意义
- P < 0.01：结果有高度统计学意义

> ⚠️ **重要提醒**: P值不是"效应大小"！P值小不等于效应大，它只说明"结果是否由随机误差引起"。

### （4）置信区间（Confidence Interval, CI）

**定义**: 包含未知总体参数的概率区间。

**常用置信区间**:
- 95% CI：最常用
- 99% CI：需要更高置信度时使用

**解读示例**:
```
某降压药使收缩压平均降低 12 mmHg（95% CI: 8-16 mmHg）

解读：有95%的把握认为该降压药使收缩压降低的真正效果在 8-16 mmHg 之间。
```

**与P值的关系**:
- 95% CI不包含0 ↔ P < 0.05
- 95% CI包含0 ↔ P ≥ 0.05

## 1.3 研究设计类型

研究设计决定了统计分析方法的选择，是统计分析的起点。

```
研究设计类型
├── 描述性研究
│   ├── 横断面研究（现况调查）
│   └── 病例报告/系列病例
├── 分析性研究
│   ├── 病例对照研究（回顾性，从结果到原因）
│   ├── 队列研究（从原因到结果）
│   └── 嵌套病例对照研究
└── 实验性研究
    ├── 随机对照试验（RCT）
    ├── 非随机对照试验
    └── 自身对照试验
```

| 研究设计 | 时间方向 | 因果推断力 | 适用场景 |
|---------|---------|----------|---------|
| RCT | 前瞻 | ★★★★★ | 干预效果评价 |
| 队列研究 | 前瞻/回顾 | ★★★★ | 风险因素分析 |
| 病例对照 | 回顾 | ★★★ | 探索性研究 |
| 横断面研究 | 同时 | ★★ | 患病率调查 |

## 1.4 数据预处理

### 1.4.1 数据收集原则

| 原则 | 说明 | 医学实践 |
|------|------|---------|
| **完整性** | 避免缺失数据 | 设计CRF表，明确必填项 |
| **客观性** | 采用客观指标 | 优先使用仪器测量 |
| **准确性** | 减少测量误差 | 培训调查员，标准化操作 |
| **时效性** | 及时记录 | 随访数据定时录入 |
| **可比性** | 标准化条件 | 统一检测方法、仪器 |

### 1.4.2 数据清洗

#### 缺失值处理

| 方法 | 适用场景 | 实现方式 |
|------|---------|---------|
| **删除法** | 缺失比例<5%，且为随机缺失 | listwise deletion / pairwise deletion |
| **均值填补** | 计量资料，整体缺失率低 | 样本均值或组内均值 |
| **回归填补** | 有预测变量时 | 建立回归模型预测缺失值 |
| **多重填补** | 缺失较多，需保持变异性 | MICE算法 |
| **末次观测值结转（LOCF）** | 随访研究 | 用最后一次观测值填补后续缺失 |

> ⚠️ **注意**: 缺失机制分为完全随机缺失（MCAR）、随机缺失（MAR）、非随机缺失（MNAR）。MNAR时任何填补方法都可能产生偏倚。

#### 异常值识别

**常用方法**:

1. **箱线图法**
   - 异常值定义：< Q1 - 1.5×IQR 或 > Q3 + 1.5×IQR
   - 极端异常值：< Q1 - 3×IQR 或 > Q3 + 3×IQR

2. **Z分数法**
   - |Z| > 3 考虑为异常值
   - 适用于正态分布数据

3. **专业知识判断**
   - 如：血压超过300 mmHg 为明显错误

**处理原则**:
- 首先核实是否为录入错误 → 纠正
- 确认为真实异常值 → 敏感性分析（包含/排除分别分析）
- 报告异常值情况

#### 数据转换

| 转换方法 | 目的 | 适用情况 |
|---------|------|---------|
| 对数转换 | 改善偏态 | 右偏数据（如抗体滴度） |
| 平方根转换 | 改善方差齐性 | Poisson分布数据 |
| 平方转换 | 改善左偏 | 左偏数据 |
| Box-Cox转换 | 自动寻找最优转换 | 通用方法 |

---

# 第二部分：核心医学统计学工具

## 2.1 描述性统计工具

### 2.1.1 计量资料描述

#### （1）集中趋势指标

| 指标 | 公式/定义 | 适用条件 | 示例 |
|------|----------|---------|------|
| **算术均数** | x̄ = Σxi/n | 正态或近似正态分布 | 身高、体重（对称分布） |
| **几何均数** | G = (x₁·x₂·...·xn)^(1/n) | 等比资料、对数正态分布 | 抗体滴度、几何平均效价 |
| **中位数** | Md，将数据排序后位于50%位置的数 | 偏态分布、分布未知 | 收入、住院天数 |

**选择原则**:
- 对称分布 → 算术均数 ± 标准差
- 右偏/左偏分布 → 中位数（四分位数间距）
- 等比数列 → 几何均数

#### （2）离散趋势指标

| 指标 | 公式 | 特点 |
|------|------|------|
| **标准差 (s)** | s = √[Σ(xi-x̄)²/(n-1)] | 与均数搭配使用 |
| **方差** | s² | 标准差的平方 |
| **四分位数间距 (IQR)** | IQR = Q75 - Q25 | 与中位数搭配 |
| **极差 (R)** | R = 最大值 - 最小值 | 简单但不稳定 |
| **变异系数 (CV)** | CV = (s/x̄) × 100% | 比较不同度量衡数据 |

**报告规范**:
- 正态分布：x̄ ± s（如：168.5 ± 8.3 cm）
- 偏态分布：M (Q1, Q3)（如：住院8天 (5, 15)）

### 2.1.2 计数/等级资料描述

#### 常用指标

| 指标 | 公式 | 应用场景 |
|------|------|---------|
| **频数** | n | 计数各类别的绝对数量 |
| **构成比** | 某部分/总体 × 100% | 表示事物内部结构 |
| **率** | 某现象发生数/可能发生总数 × 比例基数 | 表示现象发生的频率 |
| **比** | 甲指标/乙指标 | 两个指标的比值 |

## 2.2 推断性统计工具

### 2.2.1 假设检验基本步骤

```
Step 1: 建立假设
├── H₀: 原假设（μ₁ = μ₂，无差异）
└── H₁: 备择假设（μ₁ ≠ μ₂，有差异）

Step 2: 确定检验水准 α
├── α = 0.05（常规）
└── α = 0.01（需更高置信度）

Step 3: 计算检验统计量
├── t值、F值、χ²值、z值等
└── 根据数据类型和研究设计选择

Step 4: 确定P值，作出推断
├── P < α → 拒绝H₀，有统计学意义
└── P ≥ α → 不拒绝H₀，无统计学意义
```

### 2.2.2 计量资料假设检验

#### t检验

| t检验类型 | 目的 | 公式 | 适用场景 |
|-----------|------|------|---------|
| **单样本t检验** | 样本均数与已知总体均数比较 | t = (x̄ - μ₀) / (s/√n) | 判断某指标是否与标准值有差异 |
| **两独立样本t检验** | 比较两组独立样本的均数差异 | t = (x̄₁ - x̄₂) / Sx̄₁-x̄₂ | 实验组vs对照组 |
| **配对t检验** | 比较配对样本的均数差异 | t = d̄ / (sd/√n) | 治疗前后、配对设计 |

**适用条件**:
- 计量资料
- 独立样本t检验还需：两组方差齐性（Levene检验）

#### 方差分析（ANOVA）

**单因素ANOVA**
```
目的：比较多组（≥3）独立样本的均数
公式：F = MS组间 / MS组内
适用：比较3种药物的降压效果
```

**两因素ANOVA**
```
目的：同时分析两个因素及其交互作用
因素A：药物类型（3水平）
因素B：剂量（2水平）
可分析：主效应A、主效应B、交互作用A×B
```

**重复测量ANOVA**
```
目的：分析同一对象多次测量的数据
时间因素：t₀（基线）、t₁（1月）、t₂（3月）、t₃（6月）
可分析：时间效应、组间效应、时间×组别交互效应
```

#### 非参数检验

**适用条件**:
- 数据不满足正态分布
- 方差不齐
- 有序分类资料
- 样本量较小

| 检验方法 | 参数检验对应 | 适用场景 |
|---------|------------|---------|
| Wilcoxon符号秩检验 | 单样本/配对t检验 | 单样本中位数、配对差值 |
| Mann-Whitney U检验 | 两独立样本t检验 | 两独立样本比较 |
| Kruskal-Wallis H检验 | 单因素ANOVA | 多组独立样本 |
| Friedman检验 | 重复测量ANOVA | 配伍组/重复测量数据 |

### 2.2.3 计数资料假设检验

#### 卡方检验（χ²检验）

**适用条件**:
- 计数资料
- 样本量足够大（期望频数 ≥ 5的格子不超过20%）
- 独立性数据

| 检验类型 | 用途 | 示例 |
|---------|------|------|
| **四格表χ²检验** | 两组率的比较 | 新药组vs安慰剂组有效率比较 |
| **行×列表χ²检验** | 多组率或构成比比较 | 三种药物有效率比较 |
| **配对χ²检验** | 配对设计的两分类数据 | 两种诊断方法的比较 |

#### Fisher精确概率法

**适用条件**:
- 样本量较小
- 理论频数<5的格子超过20%
- 四格表数据

### 2.2.4 等级资料假设检验

| 检验方法 | 适用场景 |
|---------|---------|
| **Wilcoxon符号秩检验** | 单样本/配对等级资料 |
| **Mann-Whitney U检验** | 两独立样本等级资料 |
| **Kruskal-Wallis H检验** | 多组独立样本等级资料 |

### 2.2.5 相关与回归

#### 直线相关

**Pearson相关系数（r）**
```
适用：双变量正态分布计量资料
取值范围：-1 ≤ r ≤ 1
解读：
├── |r| < 0.3：弱相关
├── 0.3 ≤ |r| < 0.7：中等相关
└── |r| ≥ 0.7：强相关
```

**Spearman秩相关系数（rs）**
```
适用：不满足正态分布的资料、等级资料
计算：将数据转换为秩次，计算秩次间的Pearson相关系数
```

#### 直线回归

**简单线性回归**
```
目的：描述两变量间的数量依存关系
方程：Ŷ = a + bX

其中：b = Σ(xi-x̄)(yi-ȳ) / Σ(xi-x̄)²（回归系数）
      a = ȳ - bx̄（截距）

适用条件：
├── X与Y存在线性关系
├── Y服从正态分布
├── 各观测值相互独立
└── 方差齐性
```

**回归系数解读**:
```
Ŷ = 80 + 0.5X
其中X为年龄（岁），Y为收缩压（mmHg）

解读：年龄每增加1岁，收缩压平均增加0.5 mmHg
```

### 2.2.6 生存分析

#### 基本概念

| 概念 | 定义 |
|------|------|
| **生存时间** | 从起点事件到终点事件所经历的时间 |
| **删失（censoring）** | 观察对象在随访结束前失访或未发生终点事件 |
| **生存率** | 某时刻存活个体占观测个体的概率 |

#### 生存分析方法

| 方法 | 用途 |
|------|------|
| **Kaplan-Meier法** | 估计生存率，制作生存曲线 |
| **Log-rank检验** | 比较两条或多条生存曲线的差异 |
| **Cox回归** | 多因素生存分析，控制混杂因素 |

**HR解读**:
```
HR = 2.5（某因素）
解读：该因素阳性者的死亡风险是阴性者的2.5倍
```

## 2.3 高级统计工具

### 2.3.1 多因素统计分析

#### 多元线性回归

```
目的：分析多个自变量与一个连续因变量的数量关系
方程：Ŷ = b₀ + b₁X₁ + b₂X₂ + ... + bpXp

用途：
├── 控制混杂因素
├── 识别独立影响因素
└── 预测
```

#### Logistic回归

**适用条件**:
- 因变量为二分类
- 样本量足够（EPV ≥ 10-15）

```
方程：logit(P) = ln[P/(1-P)] = b₀ + b₁X₁ + ... + bpXp

OR（优势比）解读：
├── OR = 1：无影响
├── OR > 1：危险因素
├── OR < 1：保护因素
└── OR的95% CI不包含1有统计学意义
```

### 2.3.2 诊断试验评价

#### 基本指标

| 指标 | 公式 | 含义 |
|------|------|------|
| **灵敏度（Sensitivity）** | a/(a+c) | 真阳性率，筛查出病人的能力 |
| **特异度（Specificity）** | d/(b+d) | 真阴性率，排除非病人的能力 |
| **假阳性率** | b/(b+d) = 1-特异度 | 误诊率 |
| **假阴性率** | c/(a+c) = 1-灵敏度 | 漏诊率 |

#### ROC曲线

| AUC值 | 诊断价值 |
|------|---------|
| 0.5-0.7 | 较低 |
| 0.7-0.9 | 中等 |
| > 0.9 | 较高 |

### 2.3.3 Meta分析

#### 基本流程

```
1. 确定研究问题（PICO）
2. 制定检索策略
3. 文献筛选与质量评价
4. 提取数据
5. 异质性检验
6. 合并效应量
7. 敏感性分析
8. 发表偏倚检测
```

#### 异质性检验

| 检验方法 | 指标 | 异质性程度 |
|---------|------|-----------|
| **Q检验** | P值 | P < 0.1提示存在异质性 |
| **I²统计量** | I² = [(Q-df)/Q] × 100% | < 25%：轻度；25-50%：中度；> 50%：高度 |

**模型选择**:
- **固定效应模型**：异质性较低时使用（I² < 50%）
- **随机效应模型**：异质性较高时使用（I² ≥ 50%）

---

# 第三部分：统计工具实操

## 3.1 SPSS实操指南

### 3.1.1 界面介绍

```
SPSS界面组成
├── 数据视图：类似Excel，存放原始数据
├── 变量视图：定义变量属性（名称、类型、标签等）
├── 输出窗口：显示分析结果
└── 语法窗口：编写和运行SPSS命令
```

### 3.1.2 常用分析操作

#### 描述性统计

```
路径：分析 → 描述统计 → 频率/描述
操作：
1. 选择变量
2. 勾选统计量（均数、标准差、中位数、四分位数等）
3. 勾选图表（直方图）
4. 点击确定
```

#### t检验

**独立样本t检验**
```
路径：分析 → 比较均值 → 独立样本T检验
操作：
1. 检验变量：放入要比较的变量
2. 分组变量：放入分组变量，点击"定义组"
3. 点击确定

结果解读：
├── 先看Levene检验P值（P > 0.05用第一行，P ≤ 0.05用第二行）
└── 再看t检验P值
```

**配对样本t检验**
```
路径：分析 → 比较均值 → 配对样本T检验
操作：选择配对变量 → 点击确定
```

#### 卡方检验

```
路径：分析 → 描述统计 → 交叉表
操作：
1. 行列分别放入分组变量和结果变量
2. 点击"统计量"，勾选"卡方"
3. 点击"单元格"，勾选"实测"和"期望"频数
4. 点击确定
```

#### Logistic回归

```
路径：分析 → 回归 → 二元Logistic
操作：
1. 因变量：放入结局变量
2. 协变量：放入自变量
3. 方法：选择Enter/Wald/stepwise等
4. 点击确定

结果解读：
├── 模型拟合优度：-2LL、Cox & Snell R²、Nagelkerke R²
├── 方程系数：B值、Wald χ²值、P值、OR值、95% CI
└── 分类表：灵敏度、特异度、符合率
```

## 3.2 R语言实操指南

### 3.2.1 基础命令

```r
# 数据导入
data <- read.csv("data.csv")       # 导入CSV文件
data <- read.table("data.txt", header = TRUE)  # 导入TXT文件

# 数据查看
head(data)        # 查看前6行
str(data)          # 查看数据结构
summary(data)      # 描述性统计

# 变量选择
data$age          # 提取age变量
data[, c("age", "sex")]  # 选择多个变量
```

### 3.2.2 常用统计函数

```r
# t检验
t.test(weight ~ group, data = data)           # 独立样本t检验
t.test(before, after, paired = TRUE)  # 配对t检验

# 方差分析
aov(outcome ~ factor, data = data)           # 单因素ANOVA

# 卡方检验
chisq.test(table(data$group, data$outcome))  # 卡方检验
fisher.test(table(data$group, data$outcome)) # Fisher精确检验

# 相关分析
cor.test(~ var1 + var2, data = data)        # Pearson相关
cor.test(~ var1 + var2, data = data, method = "spearman")  # Spearman相关

# Logistic回归
glm(outcome ~ var1 + var2, data = data, family = binomial()) %>%
  summary()
```

### 3.2.3 常用包

| 包名 | 功能 |
|------|------|
| **dplyr** | 数据操作和整理 |
| **ggplot2** | 数据可视化 |
| **survival** | 生存分析 |
| **rms** | 回归建模 |
| **meta** | Meta分析 |
| **pROC** | ROC曲线分析 |

---

# 第四部分：常见错误与注意事项

## 4.1 设计阶段常见错误

| 错误类型 | 具体表现 | 正确做法 |
|---------|---------|---------|
| **样本量不足** | 未进行样本量估算，统计效力低 | 根据效应量、显著性水平、检验效力计算样本量 |
| **未设对照组** | 单臂研究无法比较 | 设置同期对照或历史对照 |
| **未随机化** | 组间基线不均衡 | 采用随机数字表、计算机随机等方法 |
| **盲法缺失** | 主观因素影响结果 | 根据条件采用单盲，双盲 |

## 4.2 分析阶段常见错误

| 错误类型 | 具体表现 | 正确做法 |
|---------|---------|---------|
| **方法选择错误** | 偏态资料用t检验 | 非正态资料用非参数检验 |
| **多重比较问题** | 多组两两比较增加Ⅰ类错误 | 采用Bonferroni校正或SNK-q检验 |
| **忽视方差齐性** | 方差不齐时仍用t检验 | 采用校正t检验或非参数检验 |
| **忽视交互作用** | 主效应不显著就下结论 | 先分析交互作用，再分析主效应 |

## 4.3 报告阶段常见错误

| 错误类型 | 具体表现 | 正确做法 |
|---------|---------|---------|
| **只报告P值** | 仅报告P < 0.05 | 同时报告效应量和95% CI |
| **过度依赖P值** | P > 0.05就认为"无效" | 结合临床意义和置信区间综合判断 |
| **数据曳引** | 选择性报告阳性结果 | 预先制定分析计划，完整报告 |
| **忽视数据分布** | 始终用x̄ ± s描述 | 根据分布选择合适的描述方式 |

## 4.4 P值的正确理解

> ⚠️ **P值不等于效应大小**
> 
> P值反映的是"如果H₀成立，看到当前数据或更极端数据的概率"，它不能告诉你：
> - 效应有多大
> - 临床是否重要
> - 结果是否可重复

**正确报告方式**:
```
降压效果：-12 mmHg（95% CI: -16 to -8 mmHg），P < 0.001

解读：
- 效应量：平均降低12 mmHg
- 置信区间：真值很可能在-16到-8之间
- 统计学意义：不太可能由随机误差引起
```

## 4.5 多重比较校正

| 方法 | 适用场景 | 公式 |
|------|---------|------|
| **Bonferroni校正** | 保守，适用于检验数量少 | α校正 = α/n |
| **FDR校正（Benjamini-Hochberg）** | 控制假发现率，适用于检验数量多 | BH方法 |
| **Tukey HSD** | ANOVA后两两比较 | Tukey方法 |

---

# 第五部分：总结与拓展

## 5.1 统计分析方法选择总结

### 计量资料

```
计量资料方法选择
│
├─ 单样本
│   ├─ 正态 → 单样本t检验
│   └─ 非正态 → Wilcoxon符号秩检验
│
├─ 两组独立样本
│   ├─ 正态 + 方差齐 → 独立样本t检验
│   └─ 非正态/方差不齐 → Mann-Whitney U检验
│
├─ 配对样本
│   ├─ 正态 → 配对t检验
│   └─ 非正态 → Wilcoxon符号秩检验
│
├─ 多组独立样本
│   ├─ 正态 + 方差齐 → 单因素ANOVA
│   │   └─ 两两比较 → Tukey HSD / SNK-q
│   └─ 非正态/方差不齐 → Kruskal-Wallis H检验
│
└─ 重复测量
    ├─ 正态 → 重复测量ANOVA
    └─ 非正态 → Friedman检验
```

### 计数资料

```
计数资料方法选择
│
├─ 2×2表
│   ├─ n ≥ 40，期望值≥5 → χ²检验
│   └─ n < 40或期望值<5 → Fisher精确检验
│
├─ R×C表
│   ├─ 无序 → χ²检验
│   └─ 有序/等级 → Mann-Whitney U / Kruskal-Wallis
│
└─ 配对设计
    └─ 配对χ²检验 / McNemar检验
```

## 5.2 国际报告规范

| 规范 | 适用研究类型 | 核心要点 |
|------|------------|---------|
| **CONSORT** | 随机对照试验 | 样本量计算、随机化、盲法、Flow图 |
| **STROBE** | 观察性研究 | 研究设计、混杂因素报告、统计方法 |
| **PRISMA** | 系统评价/Meta分析 | 检索策略、筛选流程、证据质量 |
| **STARD** | 诊断准确性研究 | ROC曲线、置信区间 |

## 5.3 推荐学习资源

### 书籍

| 书名 | 作者 | 特点 |
|------|------|------|
| 《医学统计学》 | 孙振球 | 国内经典教材 |
| 《Statistically Analysis》 | Dale I. Forthal | 英文经典 |
| 《R语言实战》 | Robert I. Kabacoff | R语言入门 |

---

# 附录：常用统计工具速查表

## 附录A：统计方法选择决策树

```
研究问题 → 数据类型 → 组数 → 设计 → 统计方法

两组计量资料比较
├── 配对设计 → 正态 → 配对t检验
│             └─ 非正态 → Wilcoxon符号秩检验
└── 独立设计 → 正态+方差齐 → 独立样本t检验
              └─ 非正态/方差不齐 → Mann-Whitney U检验

三组及以上计量资料比较
├── 配对/重复测量 → 正态 → 重复测量ANOVA
│                 └─ 非正态 → Friedman检验
└── 独立设计 → 正态+方差齐 → 单因素ANOVA → 两两比较Tukey HSD
              └─ 非正态/方差不齐 → Kruskal-Wallis H检验

两组率/构成比比较 → χ²检验/Fisher精确检验
多组率/构成比比较 → χ²检验
等级资料比较 → 非参数检验
相关分析 → Pearson/Spearman相关
生存分析 → Kaplan-Meier + Log-rank + Cox回归
诊断试验 → ROC曲线 + AUC
```

## 附录B：常用统计公式速查

| 公式 | 用途 |
|------|------|
| t = (x̄ - μ₀)/(s/√n) | 单样本t检验 |
| t = (x̄₁ - x̄₂)/Sx̄₁-x̄₂ | 两独立样本t检验 |
| F = MS组间/MS组内 | 方差分析 |
| χ² = Σ(O-E)²/E | 卡方检验 |
| r = Σ(xi-x̄)(yi-ȳ)/√[Σ(xi-x̄)²Σ(yi-ȳ)²] | Pearson相关 |
| OR = (a/c)/(b/d) = ad/bc | Odds比 |
| HR = exp(b) | Cox回归风险比 |

## 附录C：样本量估算要点

| 指标 | 含义 | 常规取值 |
|------|------|---------|
| α（显著性水平） | Ⅰ类错误概率 | 0.05 |
| β（Ⅱ类错误概率） | 漏检概率 | 0.2（检验效力1-β=0.8） |
| δ（效应量） | 期望发现的最小差异 | 根据文献或预实验估算 |
| σ（标准差） | 数据的变异程度 | 根据文献或预实验估算 |

**两样本t检验样本量公式**:
```
n = 2 × [(Zα/2 + Zβ)² × σ²] / δ²
```

---

> **版权声明**: 本教程仅供学习参考，内容参考了孙振球《医学统计学》（第4版）、方积乾《生物统计学》及国际统计学会相关标准。
